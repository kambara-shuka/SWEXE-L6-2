<% @products.each do |e| %>
  <p>
    <%= e.name %>
    <%= e.price %>
    <%= link_to '削除', product_path(e), method: :delete %>
    <%= link_to 'カート追加', new_cartitems_path %>
    </p>
<% end %>

<p>
  <%= link_to '商品を追加', new_product_path %>
</p>
<p>
  <%= link_to 'カートを見る', show_carts_path %>
</p>
